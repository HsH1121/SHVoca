<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 앱 실행 시 스플래시 화면에 사용되는 테마 -->
    <style name="Theme.SHVOCA.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/paper</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/paper</item>
        <item name="postSplashScreenTheme">@style/Theme.SHVOCA</item>
    </style>

    <!-- 스플래시 이후 실제 앱 테마 -->
    <style name="Theme.SHVOCA" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@color/paper</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:navigationBarColor">@color/paper</item>
    </style>
</resources>
